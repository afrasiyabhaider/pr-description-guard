{
  "name": "pr-description-guard",
  "version": "1.0.0",
  "description": "Validates GitHub PR descriptions for required sections",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "package": "npm run build && cd dist && zip -r ../pr-description-guard.zip ."
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0",
    "@types/chrome": "^0.0.0"
  }
}
